<div class="container">
    <div class="jumbotron">
      <h1>Star Wars Express</h1>
      <h3>The greatest resource in the galaxy for Star Wars statistics!</h3>
      <hr>
      <a href="/"><button type="button" class="btn btn-danger btn-lg"><span class="glyphicon glyphicon-eye-open"></span> Search Characters</button></a>
      <a href="/add"><button type="button" class="btn btn-danger btn-lg"><span class="glyphicon glyphicon-plus"></span> Add New Character</button></a>
    </div>
    <div class="row">

      <div class="col-lg-12">

        <div class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title"><strong>Character Database</strong></h3>
          </div>
          <div class="panel-body" id="wellSection">
          {{#each result}}
            <div class="well">
              <h2 id="name">{{name}}</h2>
              <div id="stats">
                <h3><strong>Role:</strong> <span id="role">{{role}}</span></h3>
                <h3><strong>Age:</strong> <span id="age">{{age}}</span></h3>
                <h3><strong>Force Points:</strong> <span id="forcePoints">{{forcePoints}}</span></h3>
              </div>
            </div>
          {{/each}}
          </div>
        </div>
      </div>      
    </div>
  </div>

  
